# JiraViz

JiraViz renders an SVG program portfolio report from a Jira CSV export.

New to command-line tools? Start here:

[JiraViz CLI Usage Guide](docs/CLI_USAGE.md)

## Local Build

```bash
go build -o jiraviz .
./jiraviz -version
```

## Render a Report

```bash
go run . -input mockups/construction-company-multi-project-jira.csv -list-projects
go run . -input mockups/construction-company-multi-project-jira.csv -project PINE -out pine-report.svg
```

## Render a Six-Month Epic Schedule

```bash
go run . -input mockups/house-renovation-jira.csv -project REN -view epics-six-month -out house-renovation-epics-six-month.svg -as-of 2026-05-01
```

## Example CLI Output

These images are generated by the JiraViz CLI from the sample CSV files in `mockups/`.

### Scrum Portfolio View

Generated from `mockups/construction-company-multi-project-jira.csv` with `-project PINE -mode points`. The Important Epics section is ranked by story points.

![Pine Ridge Scrum portfolio report generated by JiraViz](docs/images/program-portfolio-report.png)

### Kanban Portfolio View

Generated from `mockups/construction-company-multi-project-jira.csv` with `-project PINE -mode issues`. The report layout is the same; only Important Epics ranking changes to planned issue count.

![Pine Ridge Kanban portfolio report generated by JiraViz](docs/images/kanban-top-epics.png)

### Next Six Months of Epics

Generated from `mockups/house-renovation-jira.csv` with `-view epics-six-month` to show epics that start, end, or remain active inside the next six-month window.

![Next six months of epics generated by JiraViz](docs/images/all-epics-six-month-gantt.png)

## Security Scanning and Release Artifacts

The GitHub Actions release workflow builds the CLI and uses Trivy to scan both the repository and the compiled binary. It uploads SARIF to GitHub code scanning, uploads scan outputs as workflow artifacts, and attaches the same files to GitHub Releases when a release is published.

Artifacts are written under `dist/`:

```text
dist/jiraviz
dist/jiraviz-security-artifacts-<version>.tar.gz
```

The security tarball contains `SHA256SUMS` plus the Trivy table, JSON, SARIF, CycloneDX SBOM, binary scan reports, and scan manifest.

The workflow fails on `CRITICAL,HIGH` Trivy findings after writing the security artifacts.

## Contributing

See [CONTRIBUTING.md](CONTRIBUTING.md) for commit message and semantic versioning rules.
